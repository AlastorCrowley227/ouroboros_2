# ==============================
# Ouroboros environment template
# ==============================
# Все значения оставлены пустыми.
# Это справочный шаблон: основной runtime-конфиг берётся из ouroboros.config.json.

# Telegram bot token (обязательно)
TELEGRAM_BOT_TOKEN=

# VCS platform: github | gitea | git
OUROBOROS_VCS_PLATFORM=

# GitHub/Gitea access token (для github обычно обязателен)
GITHUB_TOKEN=

# Репозиторий (owner/user и repo name)
GITHUB_USER=
GITHUB_REPO=

# База Gitea (например, https://gitea.example.com)
GITEA_BASE_URL=

# Явный remote URL (для gitea/git режимов)
GIT_REMOTE_URL=

# Anthropic API key (опционально, для Claude Code CLI)
ANTHROPIC_API_KEY=



# Ollama endpoint/key (для локальных LLM-вызовов)
OLLAMA_BASE_URL=
OLLAMA_API_KEY=

# Лимит бюджета в USD
TOTAL_BUDGET=

# Рабочие директории
OUROBOROS_HOME=
OUROBOROS_REPO_DIR=

# Параметры исполнения
OUROBOROS_MAX_WORKERS=
OUROBOROS_MODEL=
OUROBOROS_MODEL_CODE=
OUROBOROS_MODEL_LIGHT=
OUROBOROS_MAX_ROUNDS=
OUROBOROS_SOFT_TIMEOUT_SEC=
OUROBOROS_HARD_TIMEOUT_SEC=
OUROBOROS_DIAG_HEARTBEAT_SEC=
OUROBOROS_DIAG_SLOW_CYCLE_SEC=
OUROBOROS_WORKER_START_METHOD=
